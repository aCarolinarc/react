/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  /* configuracion para la app directory
  experimental: {
    appdir: true,
  },
  */
};

module.exports = nextConfig;
